<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".myclassroom.AnswerRoom">
    <ListView
        android:id="@+id/listViewAnswerRoom"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp"
        android:layout_marginBottom="50dp"
        android:transcriptMode="alwaysScroll"
        android:stackFromBottom="true"
        android:divider="@null"/>

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:cardCornerRadius="@dimen/fab_margin"
        android:layout_alignParentBottom="true">
        <EditText
            android:id="@+id/answerETAnswerRoom"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:padding="10dp"
            android:hint="@string/insertAnswer"
            android:background="@drawable/circle"/>
    </androidx.cardview.widget.CardView>
    <androidx.cardview.widget.CardView
        android:id="@+id/cardAnswerRoom"
        android:layout_width="40dp"
        android:layout_height="40dp"
        app:cardCornerRadius="24dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:layout_margin="6dp">
        <ImageView
            android:id="@+id/downloadAnswerRoom"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="4dp"
            android:layout_gravity="center"
            android:visibility="gone"
            android:src="@drawable/download_cloud"/>
        <ImageView
            android:id="@+id/sendAnswerRoom"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="4dp"
            android:layout_gravity="center"
            android:visibility="gone"
            android:src="@drawable/send"/>
    </androidx.cardview.widget.CardView>

</RelativeLayout>
